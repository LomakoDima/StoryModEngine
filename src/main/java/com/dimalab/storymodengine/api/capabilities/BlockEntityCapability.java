package com.dimalab.storymodengine.api.capabilities;

/**
 * Marker a {@code @Capability} data class implements to declare it attaches to every {@code
 * net.minecraft.world.level.block.entity.BlockEntity}. See {@link EntityCapability} — same
 * mechanism, different owner kind.
 */
public interface BlockEntityCapability {
}
