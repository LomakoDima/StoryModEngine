package com.dimalab.storymodengine.common.scripting.ast;

/** What a {@code raycast { }} block or {@code if raycast.X} sugar form queries for. */
public enum RaycastTarget {
    ENTITY,
    BLOCK,
    ANY
}
