package com.dimalab.storymodengine.common.scripting.diagnostics;

/** {@code ERROR} blocks compilation of the file it appears in; {@code WARNING} is reported but never blocks. */
public enum Severity {
    ERROR,
    WARNING
}
