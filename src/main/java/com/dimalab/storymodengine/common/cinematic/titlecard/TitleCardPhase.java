package com.dimalab.storymodengine.common.cinematic.titlecard;

/** Which segment of a {@link TitleCard}'s fadeIn/hold/fadeOut envelope a given tick falls in. */
public enum TitleCardPhase {
    FADE_IN,
    HOLD,
    FADE_OUT
}
