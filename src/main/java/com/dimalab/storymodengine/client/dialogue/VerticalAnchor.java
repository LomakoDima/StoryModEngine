package com.dimalab.storymodengine.client.dialogue;

/** Where {@link DialogueLayout} positions the box vertically — resolved against the live screen height at render time, never a stored pixel offset. */
public enum VerticalAnchor {
    TOP,
    CENTER,
    BOTTOM
}
