package com.dimalab.storymodengine.client.dialogue;

/** Where {@link DialogueLayout} positions the box horizontally — resolved against the live screen width at render time, never a stored pixel offset. */
public enum HorizontalAnchor {
    LEFT,
    CENTER,
    RIGHT
}
